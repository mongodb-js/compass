var Base = require('./base');

/**
 * Clause is the common class for all possible clauses, including
 * ExpressionTreeClause and LeafClause.
 *
 * @type {Base}
 */
var Clause = module.exports = Base.extend({
});
