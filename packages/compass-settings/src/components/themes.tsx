import React from 'react';

import { Body } from '@mongodb-js/compass-components';

const ThemeSettings: React.FunctionComponent = () => {
  return (
    <div>
      <Body>Select theme</Body>
    </div>
  );
};

export default ThemeSettings;
