const Reflux = require('reflux');

const MongodbJsCompassConnectActions = Reflux.createActions([
  /**
   * define your actions as strings below, for example:
   */
  'toggleStatus'
]);

module.exports = MongodbJsCompassConnectActions;
