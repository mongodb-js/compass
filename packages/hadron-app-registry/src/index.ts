import { AppRegistry, globalAppRegistry } from './app-registry';
import type { Role } from './app-registry';
export { AppRegistry, globalAppRegistry };
export {
  AppRegistryProvider,
  registerHadronPlugin,
  HadronPluginServices,
} from './react';
export type { Role };
export default AppRegistry;
