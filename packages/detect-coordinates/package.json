{
  "name": "detect-coordinates",
  "description": "Geo coordinates detection based of a mongodb-schema type.",
  "author": "Thomas Rueckstiess <thomas@rueckstiess.net>",
  "version": "0.1.0",
  "scripts": {
    "check": "npm run lint && npm run depcheck",
    "test": "mocha",
    "test-check-ci": "npm run check && npm test",
    "lint": "eslint \"./{src,lib,test,bin}/**/*.{js,jsx,ts,tsx}\" \"./*.js\" --no-error-on-unmatched-pattern",
    "depcheck": "depcheck || echo \"!!! Dependency check failed, but the failure is ignored by now. This should be addressed in COMPASS-4772 !!!\""
  },
  "homepage": "http://github.com/mongodb-js/detect-coordinates",
  "repository": {
    "type": "git",
    "url": "git://github.com/mongodb-js/detect-coordinates.git"
  },
  "dependencies": {
    "lodash": "^4.13.1"
  },
  "devDependencies": {
    "depcheck": "^1.4.0",
    "eslint": "^7.25.0",
    "eslint-config-mongodb-js": "^2.2.0",
    "mocha": "^3.1.2",
    "pre-commit": "^1.1.2"
  },
  "license": "Apache-2.0"
}
