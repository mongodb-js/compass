var mongodbConnectionFixture = require('../');
var assert = require('assert');

describe('mongodb-connection-fixture', function() {
  it('should work', function() {
    assert(mongodbConnectionFixture);
  });
});
