import Reflux from 'reflux';

const LicenseActions = Reflux.createActions([
  /**
   * define your actions as strings below, for example:
   */
  'toggleStatus'
]);

export default LicenseActions;
export { LicenseActions };
