function activate(): void {
  // noop
}

function deactivate(): void {
  // noop
}

export { CompassInstanceStorePlugin } from './plugin';
export { activate, deactivate };
export { default as metadata } from '../package.json';
