import Reflux from 'reflux';

const ImportExportActions = Reflux.createActions([
  /**
   * define your actions as strings below, for example:
   */
  'toggleStatus'
]);

export default ImportExportActions;
export { ImportExportActions };
