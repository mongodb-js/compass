/**
 * We don't have anything in the state yet.
 */
const INITIAL_STATE = {};

/**
 * The reducer.
 *
 * @param {Object} state - The new state or the initial state.
 *
 * @returns {Object} The state.
 */
export default function reducer(state = INITIAL_STATE) {
  return state;
}
