import store from 'stores';

describe('LoadingStore [Store]', () => {
  context('when a change status ipc event is fired', () => {
    it('dispatches the change status action', () => {

    });
  });
});
