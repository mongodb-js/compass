import configureStore, {
  refreshInput,
  setDataProvider,
  setNamespace,
  setIsReadonly
} from './store';
export default configureStore;
export {
  refreshInput,
  setDataProvider,
  setNamespace,
  setIsReadonly
};
