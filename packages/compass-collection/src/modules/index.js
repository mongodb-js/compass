import { combineReducers } from 'redux';

/**
 * appRegistry,
 * dataService,
 * serverVersion,
 * tabs: [
 *   {
 *     namespace: 'db.coll',
 *     isSelected: true,
 *     isReadonly: false,
 *     isCapped: false,
 *     activeTabIndex: 3,
 *   }
 * ]
 */
const reducer = combineReducers({});

export default reducer;
