// import largeFile from './components/largeFile';

// webpack hacky - delete
// if (!global.document) {
//   global.document = global.document as any;
// }

import Workspace from './components/Workspace';

// const Workspace = 'aaaa';

// console.log('large file', largeFile);

export default Workspace;

