describe('PipelineWorkspace [Component]', () => {
  it('renders the wrapper div', () => {
    // @todo: Durran: figure out testing the dnd context.
    // expect(component.find(`.${styles['pipeline-workspace']}`)).to.be.present();
  });
});
