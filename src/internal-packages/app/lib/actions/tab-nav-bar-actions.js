const Reflux = require('reflux');

// Currently a placeholder
// Integrate when namespaceStore replaces hadron-reflux-store
const SortableTableActions = Reflux.createActions([
  'changeTabs'
]);

module.exports = SortableTableActions;
