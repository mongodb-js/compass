const Reflux = require('reflux');

const InstanceHeaderActions = Reflux.createActions([
  /**
   * define your actions as strings below, for example:
   */
  'clickInstance'
]);

module.exports = InstanceHeaderActions;
